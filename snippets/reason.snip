snippet     rc
abbr        reducer component
options     head
  type state = {
    ${1}
  };

  type action =
    | ${2}

  let component = ReasonReact.reducerComponent(__MODULE__);

  let make = (_children) => {
    ...component,

    initialState: () => ${3}

    reducer: (action, state) =>
      switch (action) {
      | _ => ReasonReact.NoUpdate()
      },

    render: self => {
      ${4}
    },
  };
